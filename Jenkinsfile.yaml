pipeline:
agent:
  any:
  stages:
    - stage "Stage 1"
      steps:  
        - sh 'java -version'
	- sh 'git --version'
    - stage "Stage 2"
      steps:
        - sh 'Yaml'
    - stage "Final"
      steps:
	- sh "echo Jenkins yaml"
